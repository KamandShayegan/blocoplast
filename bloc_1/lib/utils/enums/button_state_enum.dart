enum ButtonState {
  ///This enum is to determine whether the modal bottom sheet is for *delete* or
  ///*add* functionality.
  ///(setting a boolean was also an option but enum was seleteced for further
  ///functionalities)
  delete,
  add,
}
